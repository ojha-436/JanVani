services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: janvaani
      POSTGRES_PASSWORD: janvaani
      POSTGRES_DB: janvaani
    ports:
      - "5432:5432"
    volumes:
      - janvaani_pgdata:/var/lib/postgresql/data

volumes:
  janvaani_pgdata:
