# Copy to .env and adjust if needed.
DATABASE_URL=postgresql+psycopg://janvaani:janvaani@localhost:5432/janvaani
CORS_ORIGINS=http://localhost:3000

# Firebase Console -> Project Settings -> Service Accounts -> Generate new private key.
# Save the downloaded file as backend/serviceAccountKey.json (gitignored).
GCP_CREDENTIALS_PATH=serviceAccountKey.json
